- hosts: "{{ clients }}"
  become: true
  roles:
    - "../Share"
  vars:
    srv_name: "dfs2.t8.corp"
    share_name: '\15_Proizvodstvo'
    script_name: "{{ srv_name }}{{ share_name|regex_replace('\\\\', '-') }}"