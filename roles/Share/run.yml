- hosts: "{{ clients }}"
  become: true
  roles:
    - "../Share"
  vars:
    srv_name: "dfs2.t8.corp | upper"
    share_name: '\15_Proizvodstvo\all'
    script_name: "{{ srv_name }}{{ share_name|regex_replace('\\\\', '-') }}"